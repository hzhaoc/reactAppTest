import { createContext } from 'react';

export const ClauseLevel = createContext(0);
export const ClauseTitle = createContext(null);
export const CtxMention = createContext(null);
export const CtxMentionSetter = createContext(null);
